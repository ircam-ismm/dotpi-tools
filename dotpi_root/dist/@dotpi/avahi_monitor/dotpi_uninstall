#!/bin/bash

if [[ "$USER" != "root" ]]; then
  dotpi echo_error "This command must be run as root"
  exit 1
fi

service_name="dotpi_avahi_monitor.service"

dotpi echo_info "Uninstall ${service_name}"
dotpi service_uninstall "${service_name}"
